<?php
session_start();

// MOCK DATA — for display only
$_SESSION['username'] = 'j se';
$_SESSION['created_at'] = '2025-07-02';

$username = $_SESSION['username'];
$created_date = date("l j F, Y", strtotime($_SESSION['created_at']));
?>

<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Passenger Dashboard - LankaTransit</title>

  <!-- Bootstrap & Icons -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet" />
  <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css" rel="stylesheet" />

  <style>
    :root {
      --primary-dark: #060725;
      --accent-color: #f1424f;
      --bg-color: #f8f8f8;
      --light-card: #ffffff;
    }

    body {
      background-color: var(--bg-color);
      font-family: 'Segoe UI', sans-serif;
      color: var(--primary-dark);
    }

    .greeting-box {
      background-color: var(--light-card);
      border-left: 6px solid var(--accent-color);
      padding: 30px;
      border-radius: 12px;
      margin-top: 50px;
      box-shadow: 0 6px 16px rgba(0, 0, 0, 0.05);
    }

    .section-box {
      margin-top: 30px;
      background: var(--light-card);
      border-radius: 12px;
      padding: 25px;
      box-shadow: 0 6px 18px rgba(0, 0, 0, 0.06);
    }

    .section-box h5 {
      font-weight: 600;
      margin-bottom: 20px;
      color: var(--primary-dark);
    }

    table.table th {
      background-color: var(--primary-dark);
      color: white;
      font-weight: 600;
    }

    table.table td {
      font-size: 15px;
      color: #444;
    }

    .text-center {
      color: #888;
    }

    .btn-lanka {
      background-color: var(--accent-color);
      color: white;
      border: none;
      border-radius: 6px;
      padding: 8px 16px;
      font-size: 14px;
      transition: background 0.3s ease;
    }

    .btn-lanka:hover {
      background-color: #d63a42;
    }

    .receipt-card, .rebook-card {
      display: flex;
      justify-content: space-between;
      align-items: center;
      background-color: #fefefe;
      padding: 16px 20px;
      border-radius: 10px;
      box-shadow: 0 3px 8px rgba(0,0,0,0.05);
    }

    .receipt-card i, .rebook-card i {
      font-size: 1.5rem;
      color: #666;
    }

    .receipt-card + .receipt-card,
    .rebook-card + .rebook-card {
      margin-top: 15px;
    }

    @media (max-width: 768px) {
      .greeting-box,
      .section-box {
        padding: 20px 15px;
      }

      table th, table td {
        font-size: 13px;
      }

      .receipt-card,
      .rebook-card {
        flex-direction: column;
        align-items: flex-start;
        gap: 10px;
      }

      .btn-lanka {
        width: 100%;
      }
    }
  </style>
</head>
<body>
  <div class="container">
    <!-- 👋 Greeting -->
    <div class="greeting-box">
      <p class="mb-0">Hello, Good <?php echo (date('H') < 12) ? 'Morning' : ((date('H') < 18) ? 'Afternoon' : 'Evening'); ?>,</p>
      <h4 class="fw-bold mb-1"><?php echo htmlspecialchars($username); ?></h4>
      <p class="mb-0">Passenger since <?php echo $created_date; ?></p>
      <p class="mt-2 text-muted">
        Welcome to your dashboard. Here you can track your bookings, rebook past trips, and download receipts — all in one place with <strong>LankaTransit</strong>.
      </p>
    </div>

    <!-- 📋 Booking History -->
    <div class="section-box">
      <h5><i class="bi bi-journal-text me-2"></i>My Booking History</h5>
      <table class="table table-bordered">
        <thead>
          <tr>
            <th>Booking ID</th>
            <th>Bus No.</th>
            <th>Date</th>
            <th>From</th>
            <th>To</th>
            <th>Booked Seats</th>
            <th>Fare (LKR)</th>
          </tr>
        </thead>
        <tbody>
          <tr>
            <td colspan="7" class="text-center">No booked ticket found</td>
          </tr>
        </tbody>
      </table>
    </div>

    <!-- 🔁 Rebooking -->
    <div class="section-box">
      <h5><i class="bi bi-arrow-repeat me-2"></i>Rebooking</h5>
      <div class="rebook-card">
        <div class="d-flex align-items-center gap-3">
          <i class="bi bi-calendar-check-fill"></i>
          <div>
            <p class="mb-1 fw-semibold text-dark">Colombo → Kandy</p>
            <small class="text-muted">Last traveled: 21 June 2025 • Seat No: 12A</small>
          </div>
        </div>
        <button class="btn btn-lanka">Rebook Trip</button>
      </div>
      <p class="mt-3 text-muted small">You can rebook your last journey with just one click.</p>
    </div>

    <!-- 🧾 Receipts -->
    <div class="section-box">
      <h5><i class="bi bi-receipt me-2"></i>Receipts</h5>
      <div class="receipt-card">
        <div class="d-flex align-items-center gap-3">
          <i class="bi bi-file-earmark-text-fill"></i>
          <div>
            <p class="mb-1 fw-semibold">Booking #LTX8933</p>
            <small class="text-muted">Date: 21 June 2025 • Colombo → Kandy</small>
          </div>
        </div>
        <button class="btn btn-lanka">Download PDF</button>
      </div>
      <p class="text-muted small mt-3">Download your ticket receipts in PDF format for reference or refunds.</p>
    </div>
  </div>
</body>
</html>
